/* In greeting.js, define a function called greet that takes a name as a parameter and returns 
a personalized greeting message.

Export the greet function using the Node.js module system. */

export function greet(name) {
    return `Hello, ${name}! Welcome to Node.js.`;
};
